import SwiftUI

struct ContentView: View {

    var body: some View {
        Button("Fetch and Print Records") {
            print("Fetch and Print Records")
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}

